# /.github/workflows — GitHub Actions CI/CD pipelines
# Examples:
#   ci.yml     → Lint + test + build on every PR
#   cd.yml     → Deploy to staging/production on merge to main
#   codeql.yml → Automated security vulnerability scanning
